const a = 10;
const b = 3;

/* Adição (a + b) */
console.log(a+b);

/* Subtração (a - b) */
console.log(a-b);

/* Multiplicação (a * b) */
console.log(a*b);

/* Divisão (a / b) */
console.log(a/b);

/* Módulo (a % b) */
console.log(a%b);